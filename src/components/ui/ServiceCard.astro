---
import Icon from '@ui/Icon.astro';

interface Props {
  title: string;
  description: string;
  iconPath: string;
  iconBoxClass: string;
}

const { title, description, iconPath, iconBoxClass } = Astro.props;
---

<article class="service-card-hover relative bg-white rounded-[20px] p-8 border border-ink-200 shadow-elev-1 hover:border-curious-blue-200 hover:shadow-elev-2 hover:-translate-y-1 transition-all duration-300 overflow-hidden fade-up">
  <div class={`w-[52px] h-[52px] rounded-[10px] flex items-center justify-center mb-5 ${iconBoxClass}`} aria-hidden="true">
    <Icon path={iconPath} size={24} />
  </div>
  <h3 class="font-display text-[20px] font-bold text-ink-900 mb-2">{title}</h3>
  <p class="text-[14.5px] text-ink-500 leading-[1.65]">{description}</p>
</article>
